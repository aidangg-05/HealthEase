<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthEase"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAyQln53WiDWpdWBCzjjsy8kTFee2JFmG4" />

        <activity
            android:name=".Registration"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup_patient_1" />
        <activity android:name=".Signup_patient_2" />
        <activity android:name=".Signup_patient_3" />
        <activity android:name=".Signup_doctor1" />
        <activity android:name=".Signup_doctor2" />
        <activity android:name=".Signup_doctor3" />
        <activity android:name=".Signin" />
        <activity android:name=".Signin_Patient" />
        <activity android:name=".Signin_doctor" />
        <activity android:name=".Home_patient" />
        <activity android:name=".Home_doctor" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".appointmentPatient" />
        <activity android:name=".dietsPatients" />
        <activity android:name=".Patients_BookAppointment" />
        <activity android:name=".Patients_DoctorContact" />
    </application>

</manifest>